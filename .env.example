# The frontend no longer talks to Supabase or Firebase directly — it only calls this
# app's own /api/* endpoints (see server/.env.example for the real configuration).
# This file is intentionally empty; VITE_API_URL is only needed for local dev if your
# API server runs on a non-default port.
VITE_API_URL=http://localhost:5000/api
